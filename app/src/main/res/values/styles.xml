<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimaryHitchUs</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDarkHitchUs</item>
        <item name="colorAccent">@color/colorAccentHitchUs</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppThemeSplash" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="colorPrimary">@color/colorPrimaryHitchUs</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDarkHitchUs</item>
        <item name="colorAccent">@color/colorAccentHitchUs</item>
    </style>

    <style name="MyButton" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">@color/colorAccentHitchUs</item>
        <item name="colorButtonNormal">@color/colorPrimaryHitchUs</item>
    </style>

</resources>
